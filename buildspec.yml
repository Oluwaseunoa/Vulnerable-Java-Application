version: 0.2
phases:
  install:
    runtime-versions:
      java: latest
  build:
    commands:
      - mvn verify sonar:sonar -Dsonar.projectKey=seun-technologies -Dsonar.organization=seun-technologies -Dsonar.host.url=https://sonarcloud.io -Dsonar.token=$SONAR_TOKEN 